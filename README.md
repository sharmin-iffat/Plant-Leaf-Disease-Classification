# Plant-Leaf-Disease-Classification
Deep learning-based plant leaf disease classifier using EfficientNet-B0 and transfer learning (~90% accuracy). Includes preprocessing (resizing, normalization) and augmentation (rotation, flipping). Fine-tuned in PyTorch with Adam and CrossEntropy loss, with real-time prediction using OpenCV.
